# 未完旅箋 — k3s 部署參數（非機密）
# 複製成 deploy.env 後改成你自己環境的值；deploy.env 已被 .gitignore。
# 機密值請走 kubectl create secret，不要寫在這裡。
#
#   cp deploy.env.example deploy.env
#
# 這個檔案裡的都是 placeholder。真實部署環境的值請記在別處 —— 主機位址、
# 對外網域與同機其他站台屬於基礎設施資訊，不該進應用程式的 repo。

# 對外網域。host 上的反向代理會以此 server_name 收 HTTPS 再轉給 Service。
# 必須與 Discord Developer Portal 的 Redirect URI 完全一致。
# 若是國際化網域（IDN），三處都必須用 punycode 形式且完全一致：
# Discord Redirect URI、nginx server_name、certbot。
APP_HOST=journey.example.com

# 映像 tag。每次改版請遞增，不要重用 latest：
# k3s 用 imagePullPolicy=IfNotPresent，同名 tag 不會重新載入。
IMAGE_TAG=1.0.0

# Service 的 NodePort。這份部署假設叢集沒有 ingress controller，由 host 上的
# nginx 直接 proxy_pass 到這個埠。k8s 預設 NodePort 範圍 30000–32767。
APP_NODEPORT=30300

# 節點自己的內網位址（CIDR，通常是 /32）。app.yaml 的 NetworkPolicy 要放行它：
# NodePort 流量經 kube-proxy SNAT 之後來源是節點 IP，不放行的話 nginx 連不進來。
# 沒有通用預設值，必須填。查法：
#   kubectl get node -o jsonpath='{.items[0].status.addresses[?(@.type=="InternalIP")].address}'
NODE_CIDR=10.0.0.1/32

# 資料量（SQLite + 上傳圖片）
DATA_SIZE=10Gi

# 選填。update.sh 部署完成後會逐一 smoke-test 這些網域，用來確認「改動沒有波及
# 同一台 host 上的其他站台」。空字串就跳過這項檢查。
# 以空白分隔，例如："a.example.com b.example.com"
NEIGHBOR_HOSTS=""

# update.sh 部署成功後會在檔尾自動追加 DEPLOYED_COMMIT=<sha>，記錄「線上這個映像是用哪個
# commit 建的」。下次執行就是靠它跟 HEAD 比對來決定要不要重新部署 —— 不能改看「這次
# git pull 有沒有拉到新 commit」，因為使用者常常會自己先 pull 過。這裡不用預設值：
# 沒有這個鍵時 update.sh 一律視為需要部署，是安全的方向。
